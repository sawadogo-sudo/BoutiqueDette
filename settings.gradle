pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// SUPPRIMEZ complètement dependencyResolutionManagement pour éviter les conflits
/*
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
*/

rootProject.name = "BoutiqueDette"
include ':app'